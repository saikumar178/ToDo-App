# 1. Build Vite app
FROM node:18 AS build
WORKDIR /app
COPY .env .env
COPY package*.json ./
COPY .env .env
RUN npm install
COPY . .
RUN npm run build

# 2. Serve with NGINX
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
